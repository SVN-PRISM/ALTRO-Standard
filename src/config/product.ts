/**
 * Продуктовый режим сборки. STENCIL-only: клиентский LIBRA-пайплайн (AltroOrchestrator / runScan) отключён.
 * См. SVN_STANDARD.md — контейнер и UI сфокусированы на ALTRO STENCIL.
 */
export const ALTRO_PRODUCT_STENCIL_ONLY = true;
